{
  "name": "cloudflare-worker-search-proxy-cache",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production",
    "dev": "wrangler dev --env staging",
    "start": "wrangler dev --env staging",
    "test": "vitest",
    "test:deploy": "vitest --watch=false",
    "cf-typegen": "wrangler types",
    "predeploy": "yarn run test:deploy",
    "predeploy:production": "yarn run test:deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.12.1",
    "@vitest/coverage-v8": "3.0.9",
    "typescript": "5.9.3",
    "vitest": "3.2.4",
    "wrangler": "4.59.1"
  }
}
